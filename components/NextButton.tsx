const NextButton = () => {
  return (
    <button
      className="button hover:bg-marine-blue/80 bg-marine-blue"
      type="submit"
    >
      Next Step
    </button>
  );
};

export default NextButton;
